# Secret key used to sign JWTs. Set this to a long random string in production.
JWT_SECRET=change-this-to-a-long-random-secret

# Port the HTTP server listens on. Defaults to 8080 if not set.
PORT=8080
